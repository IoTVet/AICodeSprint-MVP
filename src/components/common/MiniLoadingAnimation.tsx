// components/common/MiniLoadingAnimation.tsx
import React from 'react';

const MiniLoadingAnimation: React.FC = () => {
  return null; // Render nothing to disable the animation
};

export default MiniLoadingAnimation;
